import { FilterByTextWithAttrPipe } from './filter-by-text-with-attr.pipe';

describe('FilterByTextWithAttrPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByTextWithAttrPipe();
    expect(pipe).toBeTruthy();
  });
});
